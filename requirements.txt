# Prévisions et journal
pandas>=2.2
numpy>=1.26
statsmodels>=0.14
openpyxl>=3.1

# Interface web
streamlit>=1.64  # chat_input avec fichiers, téléchargements sans rechargement
python-dotenv>=1.0
httpx>=0.27
mcp>=1.10,<2  # mcp_direct.py utilise streamablehttp_client, retiré en 2.0
claude-agent-sdk>=0.1

# Pièces jointes (PDF) et extractions PDF
pypdf>=5.0
fpdf2>=2.8
